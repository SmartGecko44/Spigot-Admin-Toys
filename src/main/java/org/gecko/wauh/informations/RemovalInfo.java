/*package org.gecko.wauh.informations;

import org.bukkit.entity.Player;

public class RemovalInfo {
    public void showProgress(Player player, int currentProgress, int totalProgress) {
        int progressPercentage = (int) ((double) currentProgress / totalProgress * 100);
        String actionBarMessage = "Progress"
    }
}
*/